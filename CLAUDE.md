# seednr-web — SEED Northern Rivers Ontraport App

## Project Details
- **Client:** SEED Northern Rivers (slug: `seednr`)
- **Type:** Ontraport App (vanilla JS, no build step)
- **GitHub Pages:** https://itmooti-codex.github.io/seednr-web
- **VitalSync SDK:** Loaded via CDN in header code
- **Script Load Order:** config.js → utils.js → models.js → vitalsync.js → app.js

## Port Registry
**Before assigning or changing any local dev ports**, check `~/Projects/PORT-REGISTRY.md` for current assignments. Update it when adding new ports.

## Quick Commands
- `npm run dev` — Start local dev server
- `npm run parse-schema` — Re-generate types + schema reference from schema.xml

<!-- GOTCHAS:START — Auto-synced from VibeCodeApps. Do not edit manually. -->
## Critical SDK & Pattern Gotchas

These are the most important gotchas — memorize these to avoid common bugs:

- **`plugin.switchTo()` needs the INTERNAL prefixed name** (e.g. `ThcContact`), NOT `publicName` (`Contact`). The `publicName` is for UI labels and TypeScript type names only.
- **Always `.pipe(window.toMainInstance(true))`** on all `fetchAllRecords()` / `fetchOneRecord()` queries
- **SDK records have NON-ENUMERABLE properties** — `{ ...record }` and `Object.keys(record)` produce `{}` / `[]`. Use `record.getState()` to get a plain JS object with all properties.
- **Records are immutable** — never `Object.assign(record, data)` or `record.field = value`, always spread `{ ...existingData, ...newData }`
- **Subscription payloads are PLAIN objects** (no `getState()`), do NOT include `id`, and have `null`/`undefined` for unchanged fields — never blind-merge, only merge defined non-null values, and always preserve the known `id`
- **Mutations disrupt subscriptions** — after `mutation.execute()`, clean up and re-subscribe
- **Direct GraphQL `fetch()` is an alternative to SDK for complex queries** — useful for calc/aggregation queries, cross-model joins, `orderBy`, and `field()` aliasing. The SDK query builder *does* support calc queries; past failures were due to invalid field references, not SDK limitations.
- **Keep `.limit()` reasonable** — limits above ~1,000 can cause the SDK to hang. Use pagination instead.
- **Vite dev server**: root: `.` with `open: '/dev/'` — NOT `root: 'dev'` which breaks `../src/` paths
<!-- GOTCHAS:END -->

## Reference Docs

Read these files on demand when working on the corresponding task:

- **`docs/vitalsync-sdk-patterns.md`** — VitalSync queries, subscriptions, mutations, direct GraphQL API, record conversion
- **`docs/ontraport-app-workflow.md`** — Ontraport scaffolding workflow, merge fields, dynamic lists, config bridge
- **`docs/deployment.md`** — GitHub Pages deployment, cache busting, GitHub Actions
- **`docs/schema-format.md`** — Schema XML parsing reference, data types, enums, FKs
- **`docs/research-phase.md`** — Research script, data collectors, knowledge base

## Schema Workflow
1. Export schema XML from VitalStats and place at `schema/schema.xml`
2. Run `npm run parse-schema`
3. This generates:
   - `src/types/models.js` — JSDoc typedefs + MODELS metadata
   - `schema/schema-reference.json` — Full parsed schema reference
   - Updates the Schema Reference section below

## Reusable Feature Patterns

Check `docs/features/` for pre-built feature implementations that can be added to this app. Each file documents a complete feature with architecture, file list, dependencies, and implementation steps.

## Feature Contribution

When you build a significant new reusable feature in this app (e.g., a new integration, a complex UI pattern, a data pipeline), **proactively offer to document it** for reuse in future projects:

1. Create a feature doc in `docs/features/` following the template in `docs/features/README.md`
2. Include: architecture overview, all files involved, dependencies, env vars, gotchas
3. Tell the user: *"This feature could benefit other projects. I've documented it in `docs/features/`. You can contribute it back to VibeCodeApps by copying it to `../VibeCodeApps/docs/features/` and running `./scripts/sync-child.sh --all`."*

## Staying Current

This project's docs were synced from VibeCodeApps on the date in `docs/.sync-version`. If the parent project has been updated (new features, fixed gotchas, improved patterns), the user can re-sync:
```bash
cd ../VibeCodeApps && ./scripts/sync-child.sh ../seednr-web
```

<!-- SCHEMA:START — Auto-generated by parse-schema.js. Do not edit manually. -->
## Schema Reference

12 tables, 630 columns. Full details: `schema/schema-reference.json`

### Affiliation (SDK: `SeednrAffiliation`) — 3 fields
**System Information:** id(PK), created_at(ts), last_modified_at(ts)

### Product (SDK: `SeednrProduct`) — 33 fields
**Other:** id(PK), created_at(ts), delay_start, deleted(bool), description, download_limit, download_time_limit, external_id, internal_name, last_modified_at(ts), level1, level2, offer_to_affiliates(bool), price(currency), product_code, product_group, product_type, public_name, revenue(currency), setup_fee(currency), setup_fee_date, setup_fee_when, shipping, sku, subscription_count, subscription_fee(currency), subscription_unit, taxable(bool), total_purchases_count, trial_period_count, trial_period_unit, trial_price(currency), type

### Tag (SDK: `SeednrTag`) — 3 fields
**Other:** tag_id(PK), name, object_type_id

### Message (SDK: `SeednrMessage`) — 32 fields
**Other:** id(PK), autosave, clicked_count, complaints_count, created_at(ts), from, json_data(longtext), last_auto(ts), last_modified_at(ts), last_save(ts), message_body(longtext), name, not_clicked_count, not_opened_count, object_type_id, old_resource(longtext), opened_count, plaintext(longtext), reply_to_email, resource(json), send_from_email, send_out_name, send_to, sent_count, site_id, spam_score(float), subject, transactional_email(bool), type(enum:10 values), unsubscribed_count, utm_tracking, word_wrap_checkbox

### Note (SDK: `SeednrNote`) — 11 fields
**Other:** id(PK), Affiliation_id(FK→Affiliation), Comm_id(FK→Comm), Contact_id(FK→Contact), Organisation_id(FK→Organisation), date_created(ts), last_modified_at(ts), note(longtext), object_id, object_type_id, type(enum:API|Form|Incoming|Manual|Outgoing)

### Organisation (SDK: `SeednrOrganisation`) — 207 fields
**System Information:** id(PK), xero_id, hubspotid, last_note, unique_id, created_at(ts), ip_address, last_activity(ts), last_sms_sent(ts), profile_image(imageUrl), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**COMPANY INFO:** city, name, phone, state(ISO 3166-2 code as string without the country prefix. Ex: US-FL = FL), address(address), website(url), industry(enum:41 values), legal_name, description(longtext), postal_code, facebook_page, annual_revenue(enum:1m - 5m|20m - 50m|50m - 100m|5m - 20m|< 1m|> 100m), instagram_name, linked_in_page(url), twitter_handle, reference_number, you_tube_channel(url), billing_contact_id(FK→Affiliation), primary_contact_id(FK→Affiliation), number_of_employees(enum:10 - 50|1000 +|200 - 1000|50 - 200|< 10), reference_number_type
**Member Information:** logo(imageUrl), status(enum:Active|Application|Ex-Member|On Boarding|Suspended), date_joined(ts), current_month, profile_summary(longtext), org_button_image(imageUrl), last_xero_invoice, invoice_creation_status(enum:Completed|Error|Processing), generate_prorata_invoice(bool), create_next_annual_invoice(bool), date_paid_last_membership_fee(ts)
**Portal: Affiliation Management:** portal_affiliation_management_url(url), portal_affiliation_management_visits, portal_affiliation_management_published(bool), portal_affiliation_management_unique_visits
**Expression of Interest:** payment_method(enum:Pay by Credit/Debit Card|Pay on Invoice), other_questions(longtext), type_of_organisation(enum:9 values), date_expressed_interest(ts), acknowledge_membership_fee(bool), description_of_organisation(longtext)
**Benefits:** other, support_letter(bool), authority_to_fundraise(bool), grant_writing_services(bool), access_to_grant_databases(bool), project_management_advice(bool), skill_development_workshops(bool), organisational_development_resources(bool), promotional_support_of_your_activities(bool), support_in_forming_a_workers_co_operative(bool), support_to_become_an_incorporated_association(bool), marketing_community_engagement_strategy_brainstorm(bool), potential_discount_on_insurance_coverage_for_volunteers(bool), support_to_manage_reporting_and_record_keeping_obligations(bool), a_personalised_segment_on_the_seed_website_that_links_to_your_we(bool), access_to_grants_and_donations_requiring_dgr_deductible_gift_rec(bool), exploring_access_to_a_site_at_the_mullum_enterprise_gardens_for_(bool), i_d_like_my_project_to_become_a_mullum_seed_project_i_m_not_inco(bool), networking_peer_to_peer_skill_development_with_other_environment(bool), support_to_register_as_a_charity_and_or_environmental_organisati(bool)
**Objects Of Mullum SEED 1/4:** a_intergenerational_equity(bool), c_biodiversity_conservation(bool), b_the_precautionary_approach(bool), sustainable_development_object_n_a(bool), d_supporting_the_relocalisation_of_goods_and_services_within_the(bool)
**OBJECTS OF MULLUM SEED 2/4:** research_programme_object_n_a(bool), a_soil_biology_and_the_relationship_between_healthy_ecosystems_a(bool), b_practices_and_technologies_for_the_rejuvenation_and_improvemen(bool), c_monitoring_on_site_water_use_and_grey_water_management_to_mini(bool), d_the_compatibility_of_particular_plants_soil_types_and_climatic(bool)
**OBJECTS OF MULLUM SEED 3/4:** b_waste_minimisation(bool), education_objects_n_a(bool), d_water_conservation_and_reuse_systems(bool), a_the_role_of_soil_health_in_maintaining_sustainable_ecosystems(bool), g_demonstrate_best_practice_sustainable_design_and_technologies(bool), c_modelling_a_variety_of_composting_methods_that_utilise_waste_m(bool), e_the_identification_and_management_of_weed_species_incorporatin(bool), f_the_sustainable_production_of_food_in_a_manner_that_enhances_t(bool)
**Objects Of Mullum SEED 4/4:** application_objects_n_a(bool), a_the_enhancement_of_the_natural_environment(bool), b_the_optimisation_of_environmentally_sustainable_food_productio(bool), c_community_capacity_building_that_empowers_people_to_integrate_(bool), d_create_an_inclusive_environment_which_enables_people_of_all_ag(bool)
**Member Expression of Interest:** member_expression_of_interest_url(url), member_expression_of_interest_visits, member_expression_of_interest_published(bool), member_expression_of_interest_unique_visits
**Member Donation:** create_qr(bool), donation_template(enum:Expanded Template|Own Link|Standard Simple), member_donation_url(url), biline_donation_page, content_donation_page, donation_page_qr_code(imageUrl), external_donation_link(url), member_donation_visits, background_donation_page(imageUrl), member_donation_published(bool), member_donation_unique_visits
**Donation Settings:** gateway_id, invoice_id, product_id, create_gateway(bool), create_product(bool)
**Member Onboarding:** member_onboarding_url(url), member_onboarding_visits, member_onboarding_published(bool), member_onboarding_unique_visits
**DGR Information:** bsb, account, bank_status(enum:I need a bank account from Mullum SEED|My org is setting up a bank account still|Use the bank details below), sign_wellid, signing_url(url), account_name, auspiced_by_seed(bool), donations_status(enum:14 values), date_start_auspice(ts), date_auspice_signed(ts), date_expire_auspice(ts), date_review_auspice(ts), project_description(longtext), donation_receipt_code, name_of_auspice_project, signed_auspice_agreement(url), re_create_unsigned_agreement_use_sparingly(bool)
**Insurance Information:** insurance_cover(enum:Not insured with SEED|SEED|SEED public liability only), date_end_insurance(ts), date_start_insurance(ts)
**Member Folder:** drive_folderid, drive_folder_link(url), create_member_drive_folder(bool)
**Expanded Template:** section_image1(imageUrl), section_image2(imageUrl), our_purpose_text(longtext), our_purpose_image1(imageUrl), our_purpose_image2(imageUrl), our_purpose_image3(imageUrl), what_you_can_do_cta, what_you_can_do_link(url), what_you_can_do_text(longtext), what_we_are_doing_text(longtext), lower_feature_background(imageUrl)
**Portal: Profile:** portal_profile_url(url), portal_profile_visits, portal_profile_published(bool), portal_profile_unique_visits
**Portal: Auspice:** portal_auspice_url(url), portal_auspice_visits, portal_auspice_published(bool), portal_auspice_unique_visits
**Member Gift Notification:** member_gift_notification_url(url), member_gift_notification_visits, member_gift_notification_published(bool), member_gift_notification_unique_visits
**Member: Needs and Objects:** member_needs_and_objects_url(url), member_needs_and_objects_visits, member_needs_and_objects_published(bool), member_needs_and_objects_unique_visits
**Portal: RFF:** portal_rff_url(url), portal_rff_visits, portal_rff_published(bool), portal_rff_unique_visits
**Requst for Funds Information:** fund_request_count_all, fund_request_open_total(decimal), offline_donations_pending(decimal), offline_donations_received(decimal)
**Portal: Bank Details:** portal_bank_details_url(url), portal_bank_details_visits, portal_bank_details_published(bool), portal_bank_details_unique_visits
**Portal: Member Donation Page Edits:** portal_member_donation_page_edits_url(url), portal_member_donation_page_edits_visits, portal_member_donation_page_edits_published(bool), portal_member_donation_page_edits_unique_visits
**Board Info:** next_board_meeting_date(ts), next_board_meeting_time(ts), board_agenda_item_status(enum:Closed|Open)
**Portal: Impact Reports:** portal_impact_reports_url(url), portal_impact_reports_visits, portal_impact_reports_published(bool), portal_impact_reports_unique_visits
**RiverKeepers Section:** bruns_river_keepers_founding_members(bool), bruns_river_keepers_partners_supporters(bool)
**Seed Northern Rivers : Member Donation:** seed_northern_rivers_member_donation_url(url), seed_northern_rivers_member_donation_visits, seed_northern_rivers_member_donation_published(bool), seed_northern_rivers_member_donation_unique_visits
**MEmber Website Content:** menu_link_name, show_as_project(bool), show_as_campaign(bool), link_to_content_in_menu(url)

### TagSubscriber (SDK: `SeednrTagSubscriber`) — 8 fields
**Other:** id(PK), Affiliation_id(FK→Affiliation), Comm_id(FK→Comm), Contact_id(FK→Contact), Organisation_id(FK→Organisation), tag_id(FK→Tag), object_id, object_type_id

### Contact (SDK: `SeednrContact`) — 196 fields
**System Information:** id(PK), spent(decimal), twitter, facebook, linkedin, timezone(tz), hubspotid, instagram, last_note, unique_id, created_at(ts), ip_address, user_agent, last_activity(ts), last_sms_sent(ts), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Contact Information:** fax(phone), city, role, email(email), state(ISO 3166-2 code as string without the country prefix. Ex: US-FL = FL), title, address, country(countryCode), profile(imageUrl), website(url), birthday(ts), zip_code, address_2, last_name, company_id(FK→Organisation), first_name, sms_number, company_name, office_phone, affiliation_count, linkedin_profile_url(url), i_agree_to_receive_communications_from_mullum_seed_inc(bool)
**Lead Information:** gclid, score, priority(enum:High|Low|Medium), sales_stage(enum:7 values), facebook_fbc, facebook_fbp, ga_client_id, media_medium, ga_session_id, referring_page, bulk_sms_status(bool), bulk_email_status(bool), ga_session_number, members_prospects(enum:Converted Member|Engaged Interest|Initiated Contact|Leads|Negotiation Commitment|Proposal Sent), linked_in_click_id, time_since_last_activity(enum:More than a month ago (Cold)|This Week|This month|Today (Hot!))
**SMS Merge Fields:** last_inbound_sms
**Hubspot Fields:** job_title, hubspot_comment(longtext), donation_preference
**Profile Image:** tell_us_more, profile_image(imageUrl), tell_us_more_2(longtext), how_can_we_help(enum:By Physical help|Others|With Cash)
**Donation:** amount, member, monthly(bool), once_off(bool), gateway_id, invoice_id, product_id, donation_status(enum:New|Processing|Success), mentorship_interest(enum:Maybe|Not Right Now|Yes), giving_circle_inspiration
**Invoice Info:** last_invoice, offline_transfers(longtext), last_charge_amount(currency), last_total_invoice_amount(currency), total_amount_of_unpaid_transactions(currency)
**Last Credit Card Used:** last_card_type(enum:7 values), last_cc_status(enum:[object Object]|Declined|Success), last_card_number_last_4, last_card_expiration_date(ts), last_card_expiration_year, last_card_expiration_month(enum:12 values)
**Brunswick River Information:** lives_on_the_brunswick_river(bool), plays_on_the_brunswick_r_iver(bool), vision_for_the_brunswick_river(longtext), is_keen_to_assist_or_participate_with(enum:7 values), wishes_to_volunteer_to_assist_the_alliance(bool), has_the_following_skills_or_experience_that_they_can_offer_to_th(longtext)
**Vendor Details:** insurance(json), vendor_name, stall_amount(currency), payment_status(enum:Card Added|Processing|Success), donation_amount(currency), stall_selection(enum:8 values), about_your_stall(longtext), terms_conditions(bool), date_signed_terms(ts), plants_you_sell_options_as_text, location_plants_grown_options_as_text
**Terms and COnditions:** i_agree_with_all_the_terms_of_service_and_privacy_policy(bool)
**Donation Form:** donation_type(enum:Monthly|One Time), select_amount(enum:$100|$1000|$50|Other), donation_amounts(currency), giving_circle_comment
**Offline donation:** gift_amount(currency), gift_message(longtext), transfer_date(ts)
**join our community:** send_me_email_updates(bool), send_me_updates_in_text_messages(bool)
**select  your interest:** brunsrk_subscribe(bool), brunsrk_volunteer(bool), brunsrk_membership(bool), select_your_intreest(enum:Get membership and become a riverkeeper .|Subscribe to stay updated .|Volunteer)
**Subscribe to stay updated .:** boating(bool), fishing(bool), swimming(bool), municipality, mention_other(bool), donating_funds(bool), mention_others(longtext), attending_events(bool), other_activities(bool), other_recreation(bool), other_activities_explain(longtext), participating_in_educational_workshops(bool), if_the_river_had_a_voice_what_would_it_say(longtext), discussing_a_corporate_partnership_or_sponsorship(bool), do_you_live_or_work_in_the_brunswick_river_catchment(enum:No|Yes), learning_more_about_indigenous_connections_to_the_river(bool), i_m_not_sure_yet_just_keep_me_updated_on_what_is_happening(bool), which_bruns_riverkeepers_member_group_s_are_you_part_of_if_any, discussing_my_property_as_a_location_for_tree_planting_and_other(bool), participating_in_the_healthy_rivers_monitoring_program_and_citiz(bool)
**Get membership and become a riverkeeper:** weed_management(bool), riparian_revegetation(bool), make_a_financial_contribution(bool), addressing_erosion_of_river_banks(bool), fencing_my_creek_to_exclude_grazing(bool), planting_native_trees_reforestation(bool), i_would_like_to_become_a_riverkeeper(bool), habitat_and_threatened_species_restoration(bool), undertake_catchment_restoration_works_on_my_property(bool), i_would_like_my_organization_or_business_to_join_the_riverkeeper(bool), would_you_like_more_information_on_river_restoration_works_on_yo(enum:No|Yes)
**Volunteer:** i_would_like_to_volunteer_as_a_researcher(bool), i_have_the_following_skills_and_capacities_to_offer(longtext), i_would_like_to_volunteer_in_organizing_events_and_admin(bool), i_am_an_artist_and_would_like_to_perform_or_create_works_for_the(bool), i_would_like_to_volunteer_for_on_ground_action_tree_planting_or_(bool)
**Involvement:** involvement_type(enum:Care for the river|Celebrate the river|Connect to the river)
**Philanthropists Information:** ask_amount(currency), key_people(longtext), past_giving(longtext), follow_up_date(ts), type_of_funder(enum:Contacts to other funders|Corporate partnerships|Foundations|High net worth individuals|Hub to funders), committed_amount(currency), important_to_note(longtext), engagement_procedure(longtext), probability_of_success, funder_area_of_interest(longtext), program_seeking_funding(enum:Bruns River|Changemakers|Infrastructure), expected_commitment_date(ts), philanthropic_engagement_status(enum:9 values), interactions_with_seed_nr_to_date(longtext)
**Untitled:** details(longtext), location, date_time(ts), upload_photo(imageUrl)
**Membership information:** affiliation_as_active_members
**Survey FIELDS:** lga(enum:9 values), sector(enum:Academia|Business|Community/Not for proft|Government|Philanthropy|Sector), source_of_lead(enum:Both|INR|SNR), contact_re_survey(bool), issue_of_interest(enum:13 values)
**Giving circle:** Total_Donated(decimal), Giving_Circle_Donated(decimal), date_became_giving_circle_member(ts)
**Giving Portal Invoices:** giving_portal_invoices_url(url), giving_portal_invoices_visits, giving_portal_invoices_published(bool), giving_portal_invoices_unique_visits
**Giving Portal Project Voting:** giving_portal_project_voting_url(url), giving_portal_project_voting_visits, giving_portal_project_voting_published(bool), giving_portal_project_voting_unique_visits
**grants App:** admin(bool), voter(bool), applicant(bool), committee(bool), grants_app_status(enum:active|success|suspended|updating), grants_app_user_uuid

### Comm (SDK: `SeednrComm`) — 70 fields
**System Information:** id(PK), last_note, unique_id, created_at(ts), ip_address, last_activity(ts), last_sms_sent(ts), profile_image(imageUrl), last_email_sent(ts), last_call_logged(ts), last_modified_at(ts), last_sms_received(ts), last_email_received(ts)
**Comm Information:** type(enum:Board Comms|Gift Notification|Impact Report|Member Support|Request For Funds), organisation_id(FK→Organisation)
**Gift Notification Information:** gift_note, gift_amount(currency), date_received(ts), donation_type(enum:Bank Transfer|Cash), transfer_date(ts), receipt_issued(bool), donor_information_id(FK→Contact), gift_notification_status(enum:New|Received), allocation_member_preference_id(FK→Organisation)
**Request for Funds:** date_to(ts), date_from(ts), rff_status(enum:Approved|Declined|Requested), total_funds(currency), category_6_legal(currency), category_9_other(currency), project_remainder(currency), category_2_research(currency), category_7_overseas(currency), category_8_staff_team(currency), mullum_seed_admin_fee(currency), total_funds_break_down(longtext), auspice_approval_expiry(ts), due_date_of_funds_report(ts), project_campaign_manager, category_5_administration(currency), details_of_how_funds_spent(longtext), category_4_campaign_advocacy(currency), category_3_education_information(currency), category_1_on_ground_environmental_remediation(currency)
**Board Comms:** agenda_item_file(json), agenda_item_title, agenda_item_status(enum:Current|Deleted|Expired), board_meeting_date(ts), link_to_docs_folder(url), agenda_item_description(longtext), board_member_submitting_agenda_item_id(FK→Affiliation)
**Board Member Agenda Item Edit Page:** board_member_agenda_item_edit_page_url(url), board_member_agenda_item_edit_page_visits, board_member_agenda_item_edit_page_published(bool), board_member_agenda_item_edit_page_unique_visits
**Impact Report:** support(longtext), income_other(currency), total_income(currency), new_project_plans(longtext), impact_report_status(enum:Accepted|Cancelled|More Information Required|To Be Reviewed), reporting_year_end_date(ts), impacts_and_achievements, income_from_other_grants(currency), income_from_bunya_fund_donations_and_grants(currency)
**Impact Report Edit Page:** impact_report_edit_page_url(url), impact_report_edit_page_visits, impact_report_edit_page_published(bool), impact_report_edit_page_unique_visits
**Member Support Information:** total_minutes, member_support_status(enum:Completed|In Progress|New)

### Invoice (SDK: `SeednrInvoice`) — 24 fields
**Other:** id(PK), contact_id(FK→Contact), contact_name, created_at(ts), external_order_id, hidden(bool), last_modified_at(ts), last_recharged_at(ts), offer_data, recharge(bool), recharge_attempts, shipping_amount(currency), status(enum:8 values), subtotal(currency), tax_city, tax_city_amount(currency), tax_county, tax_county_amount(currency), tax_postal_code, tax_state, tax_state_amount(currency), template_id, total(currency), total_tax(currency)

### Subscription (SDK: `SeednrSubscription`) — 21 fields
**Other:** id(PK), contact_id(FK→Contact), invoice_id(FK→Invoice), product_id(FK→Product), billing_status, cancellation_date(ts), count, created_at(ts), deleted(bool), length_of_subscription_days, next_payment_amount(currency), next_payment_date(ts), orig_month_date(ts), overdue_payment_amount(currency), payment_amount(currency), payment_remaining, payments_made, payments_total, quantity, type(enum:Payment Plan|Setup Fee|Subscription), unit

### Purchase (SDK: `SeednrPurchase`) — 22 fields
**Other:** id(PK), contact_id(FK→Contact), invoice_id(FK→Invoice), product_id(FK→Product), subscription_id(FK→Subscription), affiliate_name, coupon_code, coupon_name, created_at(ts), description(longtext), discount(currency), last_modified_at(ts), level1_commission_amount(currency), level2_commission_amount(currency), name, package_id, price(currency), quantity, sku, status(enum:7 values), total_purchase(currency), type(enum:One-time purchase|Payment plan|Subscription|Trial payment)

<!-- SCHEMA:END -->

<!-- RESEARCH:START — Auto-generated by research.cjs. Do not edit manually. -->
_Run the research script to populate this section with business intelligence._
<!-- RESEARCH:END -->
